# StudyHub Shared Package

This directory is reserved for API contracts and generated frontend clients.

Kotlin and TypeScript should share contracts through generated artifacts, not by manually duplicating models.

