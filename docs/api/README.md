# StudyHub API

API contracts will be stored here.

Recommended flow:

1. Define or generate OpenAPI from `apps/server`.
2. Store the stable contract under `packages/shared/openapi`.
3. Generate frontend API client code into `packages/shared/generated`.

