spring:
  application:
    name: studyhub-server
  profiles:
    group:
      local:
        - local
    include:
      - datasource

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

